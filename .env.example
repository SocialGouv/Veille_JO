# Secrets de la veille JO — copier ce fichier en `.env` et remplir les valeurs.
# Le fichier `.env` n'est JAMAIS commité (voir .gitignore).
# Obtention de la clé : voir INSTALL.md, section « Clé d'accès PISTE ».

# Identifiants de l'application PISTE (piste.gouv.fr), souscription API Légifrance
PISTE_CLIENT_ID=
PISTE_CLIENT_SECRET=
